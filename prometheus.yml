global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "data-retrieval"
    static_configs:
      - targets: ["courier_service:8000"]

remote_write:
  - url: http://localhost:3000
    basic_auth:
      username: admin
      password: admin
